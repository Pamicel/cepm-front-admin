import SqdaQuestionForm from './sqda-question-form'

describe('@components/sqda-question-form', () => {
  it('exports a valid component', () => {
    expect(SqdaQuestionForm).toBeAComponent()
  })
})
