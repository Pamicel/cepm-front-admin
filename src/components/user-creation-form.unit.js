import UserCreationForm from './user-creation-form'

describe('@components/user-creation-form', () => {
  it('exports a valid component', () => {
    expect(UserCreationForm).toBeAComponent()
  })
})
