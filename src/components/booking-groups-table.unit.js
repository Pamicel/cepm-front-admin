import BookingGroupsTable from './booking-groups-table'

describe('@components/booking-groups-table', () => {
  it('exports a valid component', () => {
    expect(BookingGroupsTable).toBeAComponent()
  })
})
