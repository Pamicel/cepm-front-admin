import CrossingStatistics from './crossing-statistics'

describe('@components/crossing-statistics', () => {
  it('exports a valid component', () => {
    expect(CrossingStatistics).toBeAComponent()
  })
})
