import DeathSimulationTable from './death-simulation-table'

describe('@components/death-simulation-table', () => {
  it('exports a valid component', () => {
    expect(DeathSimulationTable).toBeAComponent()
  })
})
