import LockedFirmDisplay from './locked-firm-display'

describe('@components/locked-firm-display', () => {
  it('exports a valid component', () => {
    expect(LockedFirmDisplay).toBeAComponent()
  })
})
